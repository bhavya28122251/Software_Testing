# Backend - Student Management App (scaffold)

## Setup
```bash
cd backend
npm install
npm run seed   # optional: populate demo data
npm start
```

Backend starts on http://localhost:4000 and exposes REST endpoints under /api.
